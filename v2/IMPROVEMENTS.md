# Website Improvement Plan

## Done

- [x] **Tightened the About section** — each paragraph now has one job; removed repeated "backend-focused" and the duplicated tech list; para 2 leads with the IoT/hardware-meets-software hook.
- [x] **Tightened iotrack.live description** — ~90 → ~35 words; leads with what it is, names the event-driven microservice backend, dropped the "to strengthen my experience" framing.
- [x] **Tightened Chess Log description** — trimmed the Apache/systemd deployment detail (still in tags); now "running in production behind a reverse proxy."
- [x] **Removed click-to-open links on project cards** — cards are plain divs now; the footer links are the only clickable elements.
- [x] **Skills grid cleanup** — removed React/PHP/Laravel/JavaScript/Apache (commented out), reordered, semantic borderless icons.

## Next

- [x] **Added a CTA above the fold** — pulled "CV & Contact" out of the uniform nav and made it an accent pill button (scrolls to Contact, keeps active state).
- [x] **Polished the Contact section** — fixed the raw LinkedIn slug to "Christopher Farrugia", reordered the two GitHubs (professional first), and trimmed redundant descriptions.
- [x] **Reviewed the mobile layout** — layout collapses cleanly; shrank the bottom nav pill at ≤640px (smaller padding/font + `max-width` safety net) so all five labels fit on narrow phones. Verified at 414px.

## Maybe / Low priority

- [ ] **Portfolio v1 card** — mentions React/SCSS. Likely fine as-is (clearly framed as the old site, shows range), revisit only if it bothers you.
